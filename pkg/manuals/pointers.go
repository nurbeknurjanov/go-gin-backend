package manuals

/*type Person struct {
	Name string
}

func changePerson(p **Person) {
	//p = &Person{"Aida"} //not work
	//*p = Person{"Aida"} //works
	**p = Person{"Aida"} //works, but we have to put changePerson(&p)
}
func main() {
	p := &Person{"Nurbek"}
	changePerson(&p)
	//changePerson(p) //works with *p
	fmt.Println(p)
*/
